# Supabase Configuration
# Get these from your Supabase project dashboard: https://supabase.com/dashboard
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# App URL (used for OAuth redirects, email links, MCP server)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase service role key (server-side only — never expose to client)
# Required for MCP server, admin operations, and email notifications
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Anthropic API key (for AI features — description enhancement, task generation)
# Optional: users can bring their own key (BYOK) via profile settings
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Platform AI daily limit per user (default: 50, 0 = unlimited)
# Limits how many times a single user can use platform-key AI (e.g. onboarding)
# PLATFORM_AI_DAILY_LIMIT=50

# Encryption key for storing user API keys (32-byte hex string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY_ENCRYPTION_KEY=your-64-char-hex-string-here

# Webhook secret for email notification endpoint
# Used to verify requests from Supabase pg_net webhook
NOTIFICATION_WEBHOOK_SECRET=your-webhook-secret-here

# Cloudflare Turnstile CAPTCHA (optional — signup spam protection)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key-here
TURNSTILE_SECRET_KEY=your-turnstile-secret-key-here

# GitHub OAuth App (optional — enables repo browse/create from idea pages)
# Register at https://github.com/settings/applications/new
# Callback URL: ${NEXT_PUBLIC_APP_URL}/api/github/callback
GITHUB_OAUTH_CLIENT_ID=your-github-oauth-client-id
GITHUB_OAUTH_CLIENT_SECRET=your-github-oauth-client-secret

# Sentry error tracking (optional — errors silently ignored without DSN)
NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn-here
# Build-time only (for source map uploads):
# SENTRY_AUTH_TOKEN=your-sentry-auth-token
# SENTRY_ORG=your-sentry-org-slug
# SENTRY_PROJECT=your-sentry-project-slug
